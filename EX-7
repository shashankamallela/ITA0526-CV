cap = cv2.VideoCapture("video.mp4")

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break

    cv2.imshow("Normal", frame)
    cv2.waitKey(30)  # slow motion

    # Fast motion (skip frames)
    cap.read()
    cap.read()

cap.release()
cv2.destroyAllWindows()
